// Stub for 'server-only' package in test environment.
// Next.js provides this package at build time to prevent server modules from
// being imported in client bundles. In tests we stub it as a no-op.
export default {};
